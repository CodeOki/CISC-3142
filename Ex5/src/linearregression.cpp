/* Write a program that reads a file containing a pair of numbers in each line(representing x y coordinates);
at the end of reading this file compute the linear regression for this set of numbers. Please use to the
linear regression formulas mentioned in class */
#include <iostream>
#include <vector>
#include <math.h>

using namespace std;

struct coords
{
  vector<double> xCoords;
  vector<double> yCoords;
};

int main() {
  string inputfilename = "nums.txt";
  inputFile.open(inputfilename;)
}